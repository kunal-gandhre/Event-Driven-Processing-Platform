spring:
  datasource: { url: jdbc:h2:mem:events;MODE=PostgreSQL;DB_CLOSE_DELAY=-1, username: sa, password: "" }
  kafka.bootstrap-servers: unused:9092
  flyway.enabled: true
platform.messaging.provider: test

# Enable standard Elastic Common Schema (ECS) JSON logs
logging.structured.format.console: ecs

# Optional: Add your service name to the JSON metadata
spring.application.name: order-saga-service
