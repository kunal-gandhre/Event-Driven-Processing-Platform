spring:
  datasource: { url: jdbc:h2:mem:events;MODE=PostgreSQL;DB_CLOSE_DELAY=-1, username: sa, password: "" }
  kafka.bootstrap-servers: unused:9092
  flyway.enabled: true
platform.messaging.provider: test
